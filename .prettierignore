# No auto-generated files excluded — generators run prettier before writing.

# Expo regenerates this file on every `expo start` with its own formatting.
# Trying to keep it formatted causes a permanent CI conflict.
artifacts/urban-explorer/expo-env.d.ts
